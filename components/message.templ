package components

import	"auth/db/models"


templ Messages(msgs []models.MessageWithUser) {
    <div>
        for _, m := range msgs {
            <div>
                <strong>{m.Username}</strong>: {m.Content}
                <small>{m.CreatedAt.Format("15:04")}</small>
            </div>
        }
    </div>
}

